---
exo__Asset_uid: 7eb97e7d-dff7-4bd1-a14d-a9509352bb7a
exo__Asset_label: "workflow_transition rollback"
exo__Asset_isDefinedBy: "[[60967c6a-4e8a-4ee3-8922-db98b981e4f4]]"
exo__Asset_createdAt: "2026-05-31T21:30:00"
exo__Instance_class:
  - "[[11579feb-2e42-491c-af59-b89b1129a539]]"
exocmd__Grounding_type: "[[5c1a2552-d576-496d-8823-563573dd1e2f]]"
exocmd__Grounding_direction: rollback
exo__Asset_updatedAt: 2026-08-13T20:56:40
---
# workflow_transition rollback

Generic dispatcher Grounding for rollback (`isRollback=true`) workflow transitions. Resolves transition at runtime from `(assetClass, currentStatus)` against vault Workflow ABox via `GroundingExecutor.executeWorkflowTransition` (RFC 36347daf Phase 2).

Shared by Phase 1 backward Commands migrated in Phase 3:

- Re-open (Done → Doing) — postActions: clear endTimestamp + resolutionTimestamp
- Rollback to Backlog (Task: Doing → Backlog) — postActions: clear startTimestamp
- Rollback to Backlog (Project: Doing → Backlog) — `0b4330eb`, no postActions
- Rollback to Draft (Backlog → Draft) — no postActions

Per-Command target class gating is via `exocmd__CommandBinding_targetClass`; per-state visibility gating is via Phase 1 `exocmd__Precondition_sparqlAsk`.

## Изменения 2026-08-13 (задача `ceae3805-564b-4dc1-a95b-9ebfeace3194`)

Из списка убраны два потребителя, ссылавшиеся на удалённый статус `ems__EffortStatusToDo` (`6a0e933a`):

- **Rollback to ToDo** (`e21bf423`) + его биндинг на `ems__Project` (`caca72f6`) — **удалены**. После удаления `ToDo` переход `Project: Doing → ToDo` перестал существовать, и команда резолвилась в `Doing → Backlog`, продолжая обещать «ToDo» и в метке, и в successMessage. Возможность при этом НЕ потеряна: у Project остаётся «Rollback to Backlog (Project)» (`0b4330eb`), который делает ровно это и честно называется. Проверено `resolve-buttons` до/после.
- **Rollback to Analysis** (`Project: ToDo → Analysis`) — потребителя в vault больше нет.

## ⛔ Ограничение диспетчера, важное при добавлении новых команд

Переход ищется как `transitions.find(t => t.from === currentStatus && t.isRollback === isRollback)` — то есть из каждого состояния доступен ровно ОДИН forward и ровно ОДИН rollback. Боковая ветка (например «припарковать в `Waiting` из `Backlog`, у которого обе позиции заняты») этим грounding'ом невыразима — для неё используется `property_set` (см. `0e47975e`).
